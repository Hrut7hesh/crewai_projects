fetching_task:
  description: >
    1. Use the getUserStories tool to fetch issues from project {project_key}.
    2. Analyze the retrieved issues.
    3. Create a summary of who is reporting the most stories and what the common themes are.
  expected_output: >
    A structured summary report in json format of the Jira user stories found.
  agent: fetcher
